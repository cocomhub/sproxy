# 2026-05-13

## integrate-streaming spec 实现完成

完成了 `D:\workdir\leon\sproxy\.trae\specs\integrate-streaming` 规格的全部实现。

### 变更摘要

**`pkg/tunnel/tunnel.go`** — 全量重写：
- 新增 `encodeMetadataFrame` / `decodeMetadataFrame` 流式帧辅助函数
- `Client.DoRaw`：内部改为流式帧协议，签名不变
- `Client.Do`：全流式，不再调用 DoRaw，resp.Body 为流式 Reader
- `NewHandler`：全流式，decodeMetadataFrame + DecryptStream 接收，encodeMetadataFrame + EncryptStream 响应
- 删除废弃代码：`encodeFrame`、`decodeFrame`、`frameThreshold`、`httpRequestToTunnelRequest`、`tunnelResponseToHTTPResponse`、`writeEncryptedResponse`

**`pkg/tunnel/stream.go`** — 修复已有 bug：
- chunk 长度从 `uint16`（2字节）改为 `uint32`（4字节），修复 DefaultChunkSize=64KB 时密文长度 65564 > 65535 溢出问题

**`pkg/tunnel/example_test.go`** — 更新测试：
- `ExampleClient_Do_largeBody` 扩大到 128KB
- 新增 `ExampleClient_Do_streamResponse`
- `Example_tamperDetection` 改为发送损坏帧
- 全部 19 个 Example 通过

### 关键设计决策
- 所有请求/响应统一帧格式：`[4B metaLen][encryptedMeta][streamChunks...]`，无大小判断
- `DoRaw` 响应仍用 `Response.Body = base64(decrypted)` 保持向后兼容
- `Do` 的 resp.Body 是 `io.Pipe` + `DecryptStream` goroutine，调用方 Close() 无 goroutine 泄漏

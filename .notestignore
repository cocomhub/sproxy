# .notestignore — 无测试文件的包免检列表
# 语法：一行一个包路径或 glob 模式（无需 ./ 前缀，脚本会归一），支持 # 注释和空行
# 注意：`make notest` 的包列表来自 `go list ./...`，因此**新增的 production 包若无测试会直接报红**；
# 只有确实不该有测试的包才应登记在此（登记时要写清理由）。
# 随着测试覆盖率的提升，逐步移除条目

# build/ 下的产物目录（例如运行测试时留下的 build/tmp_db）不是源码，门禁不应要求测试
build/*

# 以下为无测试文件的 tools 包，暂不要求测试：
tools/genbenchview
tools/gencoverview
tools/genreport
tools/gentimingview

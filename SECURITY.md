# Security Policy

## 支持的版本

仅**最新发布版本**获得安全修复（见 [Releases](https://github.com/cocomhub/sproxy/releases)）。
更早的版本请先升级再复测。

## 报告漏洞（请勿开公开 Issue）

请通过 GitHub 的**私下**渠道报告：

**<https://github.com/cocomhub/sproxy/security/advisories/new>**
（仓库 `Security` 标签页 → `Report a vulnerability`）

也可以直接给仓库维护者发 GitHub 私信。**请勿**在公开 Issue、Discussion 或 PR 中披露漏洞细节——
在修复发布前公开会让所有部署暴露。

### 报告里请尽量包含

- 受影响的版本/提交，以及部署形态（单机 / hub + 节点、是否启用 TLS、是否启用 `access_keys`/`api_keys`）；
- 复现步骤或最小 PoC（命令、配置片段、请求样本）；
- 影响评估：能读到/改到/删掉什么，是否需要已认证身份，是否可远程触发；
- 若已知修复方向，欢迎附上建议（但不必先写出补丁）。

## 我们如何处理

- **确认**：收到后尽快确认可复现性（通常几个工作日内，best effort 而非 SLA）。
- **定级与修复**：按影响定级并在 `master` 上修复，凡涉及对外行为的修复会按发布流程出一版
  （见 [RELEASING.md](RELEASING.md)）。
- **披露**：修复版本发布后，在 GitHub Security Advisory 中公开致谢（除非你要求匿名）。
- 请给我们留出协调披露的时间窗口（默认 90 天或修复发布之日，以先到者为准）。

## 范围提示（这些设计是有意的）

排查前请先确认是否为**设计语义**而非缺陷：

- **`--insecure` 的作用域有限**：HTTP 直连面不限地址，但 xfer tcp+tls 面**仅限 loopback hub**
  （远程必须显式 `--ca-file`），这是 fail-closed 的设计而非疏漏；
- **对端身份需要显式 pin**：`peer_fingerprints` 未配置时远程只读面**拒绝连接**（不 TOFU）；
- **存储根由进程独占**：文件操作的正确性依赖「服务进程是本进程内唯一写入者」，
  外部程序直接改动 `storage_root` 不受支持（并发写入可能绕过检查）；
- **路径穿越防护**：所有用户路径经 `pathguard` 校验，`..`/绝对路径/空字节均被拒绝；
  若发现绕过方式，请**务必**私下报告。

## 已知的次要事项

以下属已知取舍，欢迎讨论但不构成安全边界内的漏洞：

- 配置文件中的密钥以明文保存（靠 `0600` 权限约束）——服务端必须读取明文才能工作；
- `docs/` 下的测试脚本与示例使用自签证书与固定测试密钥，**不得**用于生产。
